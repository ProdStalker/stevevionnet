<div class="medias-container">
    {% for media in computed.medias %}
        {{ component(
            'media',
            {
                media: media
            }
        ) }}
    {% endfor %}
</div>